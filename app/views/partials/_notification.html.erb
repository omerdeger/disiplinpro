<% if flash.any? %>
  <div class="column is-half is-offset-one-quarter">
    <% flash.each do |name, msg| %>
      <div class="<%= notification_flash(name) %>">
        <%= msg %>
      </div>
    <% end %>
  </div>
<% end %>